@using Jaydlc.Core.Models
<video controls>
    <source src="@Video.Formats.FirstOrDefault(x => x.ext == "mp4" && x.format_note == "720p").url" type="video/mp4">
</video>

@code {

    [Parameter]
    public VideoInfo? Video { get; set; }

}